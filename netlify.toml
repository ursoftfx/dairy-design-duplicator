[build]
  command = "vite build --config vite.config.netlify.ts"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# Nitro detects Netlify automatically and outputs serverless functions
# alongside the static publish directory. No redirects file needed.
